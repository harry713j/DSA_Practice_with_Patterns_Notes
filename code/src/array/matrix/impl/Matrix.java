package array.matrix.impl;

public interface Matrix {
    void insert(int row, int col, int element); // for inserting the element in the matrix
    void display(); // this is for displaying the matrix
}
